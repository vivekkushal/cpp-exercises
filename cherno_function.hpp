// These are include guards

// Include guards are used to make sure that this "cherno_function.hpp" file is included only once
#ifndef CHERNO_FUNCTION_HPP
#define CHERNO_FUNCTION_HPP

// Prototype declaration of all the functions that we are using
int Multiply(int a, int b);
void MultiplyAndLog(int a, int b);

// These are include guards
#endif