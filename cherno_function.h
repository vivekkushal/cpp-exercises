// These are include guards

// Include guards are used to make sure that this "cherno_function.h" file is included only once
#ifndef CHERNO_FUNCTION_H
#define CHERNO_FUNCTION_H

// Prototype declaration of all the functions that we want to use
int Multiply(int a, int b);
void MultiplyAndLog(int a, int b);

// These are include guards
#endif